# 网络攻击检测工作流依赖
# 注意：由于智能体平台的限制，所有依赖代码都已内嵌到模块中
# 此文件仅作为参考，实际部署时不需要安装任何外部依赖

# 标准库模块（已内嵌）
# json - JSON数据处理
# re - 正则表达式
# datetime - 时间处理
# sys - 系统相关
# os - 操作系统接口

# 如果需要在本地测试环境运行，可以安装以下依赖：
# 但这些依赖在实际部署时不会使用

# requests>=2.25.0  # HTTP请求（仅用于测试）
# pytest>=6.0.0    # 测试框架（仅用于测试）